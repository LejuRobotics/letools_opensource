# sdk_version.env - LeTools 配套 kuavo-ros-opensource SDK 版本锁定清单
#
# 此文件随 LeTools 仓库一起发版（被 git 跟踪），记录当前 LeTools 版本
# 经过验证、配套使用的 kuavo-ros-opensource 分支与该分支上的具体 tag。
#
# 两个字段必须同时存在并指向同一 commit：
#   SDK_REPO_BRANCH  SDK 来源分支（如 dev）
#   SDK_REPO_TAG     该分支上配套的 tag（如 1.4.5）
#
# LeTools 每次发版时，由维护者将此处更新为本次配套验证过的分支 + tag。
# 安装时以这两个值为准，用户可明确知道当前 LeTools 配套的 SDK 版本。
#
# 开发者如需临时调试其他版本，可在 scripts/kuavo_humanoid_sdk_tools/sdk_config.sh
# 中设置 SDK_REPO_BRANCH / SDK_REPO_TAG 显式覆盖（本地配置优先于此文件）。
SDK_REPO_BRANCH="master"
SDK_REPO_TAG="1.4.4"
